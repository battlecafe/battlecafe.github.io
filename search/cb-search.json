---
layout: null
sitemap:
  exclude: true
---
{
	"code" : 0 ,
	"data" : [
	{% for post in site.posts %}
    {
        "title": "{{ post.title | escape }} - {{ post.subtitle | escape }} - {{ post.tags | join: ' • ' }} - {{ post.date }} - {{ post.content | strip_html | strip_newlines | escape |replace:'\','@@' }}"
        "url": "{{ site.baseurl }}{{ post.url }}"
    }
    {% if forloop.rindex != 1  %}
	,
	{% endif %}
    {% endfor %}
	         ]
}