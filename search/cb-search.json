---
layout: null
sitemap:
  exclude: true
---
{
	"code" : 0 ,
	"data" : [
	{% for post in site.posts %}
    {
        "title": "{{ post.title | escape }} - {{ post.subtitle | escape }}",
        "tags": "{{ post.tags | join: ' • ' }}",
        "url": "{{ site.baseurl }}{{ post.url }}",
        "date": "{{ post.date }}",
        "content": "{{ post.content | strip_html | strip_newlines | escape |replace:'\','@@' }}"
    }
    {% if forloop.rindex != 1  %}
	,
	{% endif %}
    {% endfor %}
	         ]
}